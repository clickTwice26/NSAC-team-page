# Flask settings
SECRET_KEY=dev-secret-key-change-in-prod
FLASK_DEBUG=1
PORT=8000

# Redis credentials & connection
REDIS_PASSWORD=dev_redis_password

# Use '@redis:6379' inside Docker Compose network, or '@localhost:6379' when running Flask directly on host
REDIS_URL=redis://:dev_redis_password@redis:6379/0
